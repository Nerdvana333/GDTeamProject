using UnityEngine;

public class ScoreManager : MonoBehaviour {
    
    void Update() {

        if (GameSettings.instance.Playing) {

            // GameMonitors.instance.Score += ...;
            // References.instance.HungerPanel.fillAmount = GameMonitors.instance.Hunger;
        
        }
        
    }

}
